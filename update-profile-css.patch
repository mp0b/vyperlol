--- src/components/profile/profile.css
+++ src/components/profile/profile.css
@@ -107,7 +107,7 @@
   justify-content: center;
   min-height: 100svh;
-  padding: clamp(24px, 7vh, 84px) 16px;
+  padding: 24px 16px;
 }
 .vy-card {
   position: relative;
   isolation: isolate;
   overflow: hidden;
-  width: min(var(--vy-card-width, 420px), 92vw);
+  width: min(var(--vy-card-width, 500px), 100vw);
   margin: 0 auto;
-  padding: clamp(1rem, 4vw, var(--vy-card-padding, 24px));
-  background: var(--vy-card-bg, rgba(10, 10, 10, 0.6));
+  padding: var(--vy-card-padding, 32px) 24px;
+  background: var(--vy-card-bg, rgba(0, 0, 0, 0.45));
   border: 1px solid var(--vy-card-border, rgba(255, 255, 255, 0.1));
-  border-radius: var(--vy-card-radius, 16px);
-  box-shadow: 0 0 30px rgba(0, 0, 0, 0.8), inset 0 0 10px rgba(255, 255, 255, 0.05);
-  -webkit-backdrop-filter: blur(var(--vy-card-blur, 20px));
-  backdrop-filter: blur(var(--vy-card-blur, 20px));
+  border-radius: var(--vy-card-radius, 20px);
+  box-shadow: 0 0 40px rgba(0, 0, 0, 0.9), inset 0 0 10px rgba(255, 255, 255, 0.05);
+  -webkit-backdrop-filter: blur(var(--vy-card-blur, 25px));
+  backdrop-filter: blur(var(--vy-card-blur, 25px));
   display: flex;
   flex-direction: column;
   align-items: center;
-  gap: 15px;
+  gap: 18px;
   text-align: center;
   animation: vy-enter 0.8s cubic-bezier(0.2, 0.7, 0.2, 1) both;
 }
@@ -127,7 +127,7 @@
   width: clamp(80px, 25vw, var(--vy-avatar-size, 110px));
   height: clamp(80px, 25vw, var(--vy-avatar-size, 110px));
   border-radius: var(--vy-avatar-radius, 50%);
   object-fit: cover;
   display: block;
-  border: 2px solid rgba(255, 255, 255, 0.1);
-  background: rgba(0, 0, 0, 0.5);
-  box-shadow: 0 0 20px rgba(0, 0, 0, 0.8);
+  border: 1px solid rgba(255, 255, 255, 0.15);
+  background: #000;
+  box-shadow: 0 0 25px rgba(0, 0, 0, 0.9);
 }
@@ -142,8 +142,8 @@
 }
 
 .vy-name {
-  font-size: clamp(1.2rem, 5vw, 1.6rem);
-  font-weight: 800;
+  font-size: clamp(1.4rem, 5vw, 1.8rem);
+  font-weight: 700;
   line-height: 1.1;
   text-transform: var(--vy-transform, none);
   display: inline-flex;
@@ -179,15 +179,15 @@
 .vy-badges {
   display: flex;
   flex-wrap: wrap;
-  gap: 8px;
+  gap: 6px;
   justify-content: center;
 }
 .vy-badge {
   display: inline-flex;
   align-items: center;
   gap: 5px;
-  padding: 4px 10px;
+  padding: 2px 8px;
   border-radius: 6px;
-  font-size: 0.7rem;
+  font-size: 0.65rem;
   font-family: var(--font-mono, monospace);
   text-transform: uppercase;
-  background: rgba(0, 0, 0, 0.6);
+  background: rgba(0, 0, 0, 0.3);
   color: var(--vy-badge-color, #fff);
   border: 1px solid color-mix(in srgb, var(--vy-badge-color, #fff) 30%, transparent);
-  box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
+  box-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
   transition: all 0.2s ease;
 }
@@ -232,15 +232,15 @@
   display: flex;
   align-items: center;
   gap: 12px;
   width: 100%;
-  padding: 14px 16px;
-  border-radius: var(--vy-link-radius, 12px);
-  background: var(--vy-link-bg, rgba(20, 20, 20, 0.4));
+  padding: 12px 16px;
+  border-radius: var(--vy-link-radius, 8px);
+  background: var(--vy-link-bg, rgba(0, 0, 0, 0.4));
   color: var(--vy-link-text, #fff);
   text-decoration: none;
-  font-weight: 500;
-  font-size: 0.95rem;
-  border: 1px solid rgba(255, 255, 255, 0.05);
-  box-shadow: inset 0 0 10px rgba(0,0,0,0.5);
-  backdrop-filter: blur(10px);
+  font-weight: 600;
+  font-size: 0.9rem;
+  border: 1px solid rgba(255, 255, 255, 0.1);
+  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
+  backdrop-filter: blur(15px);
   transition: all 0.3s cubic-bezier(0.2, 0.8, 0.2, 1);
   overflow: hidden;
 }
@@ -248,7 +248,7 @@
   background: rgba(255, 255, 255, 0.05);
-  border-color: rgba(255, 255, 255, 0.2);
+  border-color: rgba(255, 255, 255, 0.3);
 }
 .vy-link-icon { display: inline-flex; flex-shrink: 0; color: var(--vy-accent, #fff); filter: drop-shadow(0 0 5px rgba(255,255,255,0.3)); }
-.vy-link-body { display: flex; flex-direction: column; align-items: flex-start; gap: 2px; min-width: 0; text-align: left; }
+.vy-link-body { display: flex; flex-direction: column; align-items: flex-start; gap: 1px; min-width: 0; text-align: left; }
 .vy-link-title { font-weight: 700; text-shadow: 0 0 10px rgba(0,0,0,0.8); }
 .vy-link-desc { font-size: 0.75rem; color: rgba(255, 255, 255, 0.5); }
